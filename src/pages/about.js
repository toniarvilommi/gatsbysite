import React from 'react'
import Layout from './components/layout'

const pageTitle = 'This is the page title'

const AboutPage = () => {
  return <Layout pageTitle={pageTitle}>Content</Layout>
}

export default AboutPage
